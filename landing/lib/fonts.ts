// Keep the Arabic font hook shape without downloading Google Fonts during a
// production build. The CSS stack uses installed Arabic-capable system fonts.
export const notoSansArabic = {
  variable: "font-arabic",
  className: "font-arabic",
};
