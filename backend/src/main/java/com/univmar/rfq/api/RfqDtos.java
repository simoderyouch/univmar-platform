package com.univmar.rfq.api;
import jakarta.validation.Valid; import jakarta.validation.constraints.DecimalMin; import jakarta.validation.constraints.NotEmpty; import jakarta.validation.constraints.NotNull; import java.math.BigDecimal; import java.time.LocalDate; import java.util.List;
public final class RfqDtos { private RfqDtos(){} public record Item(@NotNull Long variantId,@NotNull @DecimalMin("0.01") BigDecimal quantityM2,String note){} public record Create(@NotEmpty List<@Valid Item> items,Long projectId,String notes,LocalDate desiredDate){} public record Response(Long id,String status){} public record Detail(Long id,String status,Long customerId,Long projectId,String projectName,String notes,LocalDate desiredDate,Long assigneeId,List<ItemDetail> items){} public record ItemDetail(Long id,Long variantId,String materialName,String finish,BigDecimal quantityM2,String note){} public record Assign(@NotNull Long salesUserId){} }
